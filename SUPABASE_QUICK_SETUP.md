# ⚡ Быстрая настройка Supabase (у вас уже есть данные)

## ✅ Что уже готово

1. ✅ **Переменные окружения** созданы в `.env.local`
2. ✅ **Supabase проект** настроен:
   - URL: `https://fvxkywczuqincnjilgzd.supabase.co`
   - Ключи уже в файле `.env.local`

## 🚀 Что нужно сделать сейчас

### Шаг 1: Создать схему базы данных

1. Откройте Supabase Dashboard: https://fvxkywczuqincnjilgzd.supabase.co
2. Войдите в свой аккаунт
3. Перейдите в **SQL Editor** (в левом меню)

### Шаг 2: Выполнить SQL скрипты

Выполните скрипты **по порядку** (каждый в отдельном запросе):

#### 1. Профили пользователей
```sql
-- Скопируйте содержимое scripts/001_create_profiles.sql
-- И выполните в SQL Editor
```

#### 2. Фонды
```sql
-- Скопируйте содержимое scripts/002_create_funds.sql
```

#### 3. Кампании
```sql
-- Скопируйте содержимое scripts/003_create_campaigns.sql
```

#### 4. Пожертвования
```sql
-- Скопируйте содержимое scripts/004_create_donations.sql
```

#### 5. Обновления кампаний
```sql
-- Скопируйте содержимое scripts/005_create_campaign_updates.sql
```

#### 6. Подписки
```sql
-- Скопируйте содержимое scripts/006_create_subscriptions.sql
```

#### 7. Отчёты
```sql
-- Скопируйте содержимое scripts/007_create_reports.sql
```

#### 8. ⭐ RPC функции (ВАЖНО!)
```sql
-- Скопируйте содержимое scripts/009_create_rpc_functions.sql
-- Это обязательно для работы пожертвований!
```

#### 9. Тестовые данные (опционально)
```sql
-- Скопируйте содержимое scripts/008_seed_initial_data.sql
-- Это добавит тестовые фонды
```

### Шаг 3: Проверка

После выполнения всех скриптов проверьте:

1. **Таблицы созданы**:
   - Database → Tables
   - Должны быть: `profiles`, `funds`, `campaigns`, `donations`, `campaign_updates`, `subscriptions`, `reports`

2. **RPC функции созданы**:
   - Database → Functions
   - Должны быть: `increment_total_donated`, `increment_campaign_amount`, `increment_fund_amount`

3. **RLS политики активны**:
   - Authentication → Policies
   - Должны быть политики для всех таблиц

### Шаг 4: Настроить Storage (опционально)

1. Перейдите в **Storage** (в левом меню)
2. Создайте bucket `campaign-images`:
   - Public bucket: ✅
   - File size limit: 5 MB
   - Allowed MIME types: `image/jpeg,image/png,image/webp`
3. Создайте bucket `user-avatars`:
   - Public bucket: ✅
   - File size limit: 2 MB
   - Allowed MIME types: `image/jpeg,image/png,image/webp`

## ✅ Готово!

Теперь можно запустить приложение:

```bash
pnpm dev
```

Приложение подключится к Supabase и будет работать с реальной базой данных!

---

## 🔍 Быстрая проверка

Откройте в браузере:
- http://localhost:3000 - должно работать без ошибок
- http://localhost:3000/funds - должны отображаться фонды (если выполнили seed)
- http://localhost:3000/campaigns - страница кампаний

---

## ❓ Если что-то не работает

1. **Проверьте `.env.local`** - файл должен быть в корне проекта
2. **Проверьте подключение**: в Supabase Dashboard → Settings → API
3. **Проверьте логи**: в консоли браузера и терминале
4. **Убедитесь, что все SQL скрипты выполнены** - особенно `009_create_rpc_functions.sql`

---

**Следующий шаг**: После настройки БД подключите реальные данные на страницах (см. `PROJECT_STATUS.md`)

