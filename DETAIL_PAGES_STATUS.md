# 📄 Статус детальных страниц

## 🔴 Детальные страницы, НЕ подключённые к БД:

### 1. **Детальная страница кампании** 
**Путь**: `app/campaigns/[id]/page.tsx`  
**URL**: `/campaigns/[id]` (например, `/campaigns/123`)

#### ❌ Текущее состояние:
- Использует **полностью моковые данные** (хардкод)
- TODO комментарий на строке 19: `// TODO: Fetch campaign from database`
- Объект `campaign` создан вручную (строки 20-58)

#### ✅ Что уже есть:
- Функция `getCampaignById(id)` в `lib/actions/campaigns.ts` (строка 88)
- Server Action готова к использованию

#### 📋 Что нужно сделать:
1. Импортировать `getCampaignById` из `lib/actions/campaigns.ts`
2. Заменить моковый объект на:
   ```typescript
   const result = await getCampaignById(id)
   if (result.error || !result.campaign) {
     notFound()
   }
   const campaign = result.campaign
   ```
3. Преобразовать формат данных из БД в формат компонента
4. Получить обновления кампании из `campaign_updates` таблицы
5. Получить недавние пожертвования из `donations` таблицы

#### ⏱️ Оценка времени: 2-3 часа

---

### 2. **Детальная страница фонда**
**Путь**: `app/funds/[id]/page.tsx`  
**URL**: `/funds/[id]` (например, `/funds/456`)

#### ❌ Текущее состояние:
- Использует **полностью моковые данные** (хардкод)
- TODO комментарий на строке 19: `// TODO: Fetch fund from database`
- Объект `fund` создан вручную (строки 20-48+)

#### ✅ Что уже есть:
- Функция `getFundById(id)` в `lib/actions/funds.ts` (строка 32)
- Server Action готова к использованию

#### 📋 Что нужно сделать:
1. Импортировать `getFundById` из `lib/actions/funds.ts`
2. Заменить моковый объект на:
   ```typescript
   const result = await getFundById(id)
   if (result.error || !result.fund) {
     notFound()
   }
   const fund = result.fund
   ```
3. Преобразовать формат данных из БД в формат компонента
4. Получить недавние пожертвования из `donations` таблицы
5. Получить проекты фонда (если есть отдельная таблица)

#### ⏱️ Оценка времени: 2-3 часа

---

## ✅ Детальные страницы, которые НЕ существуют (нет в проекте):

В проекте **нет** детальных страниц для:
- ❌ Детальная страница пожертвования (`/donations/[id]`)
- ❌ Детальная страница пользователя/профиля другого пользователя (`/users/[id]`)
- ❌ Детальная страница отчёта (`/reports/[id]`)

Эти страницы не планировались, поэтому их отсутствие не является проблемой.

---

## 📊 Сводка

| Страница | URL | Статус | Функция БД | Время на исправление |
|----------|-----|--------|------------|---------------------|
| Кампания | `/campaigns/[id]` | ❌ Моковые данные | ✅ `getCampaignById()` | 2-3 часа |
| Фонд | `/funds/[id]` | ❌ Моковые данные | ✅ `getFundById()` | 2-3 часа |

---

## 🎯 Итого

**Детальные страницы, НЕ подключённые к БД: 2**

1. `/campaigns/[id]` - детальная страница кампании
2. `/funds/[id]` - детальная страница фонда

**Обе страницы имеют готовые Server Actions**, нужно только:
- Импортировать функции
- Заменить моковые данные на вызовы функций
- Преобразовать формат данных

**Общее время на исправление: 4-6 часов**

---

**Последнее обновление**: 2025-01-15

