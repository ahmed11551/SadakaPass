# 📦 Руководство по переносу проекта в новый репозиторий

## ✅ Что НЕ нужно менять:

1. **package.json** - имя проекта можно оставить как есть или изменить на "mubarakway"
2. **next.config.mjs** - конфигурация универсальна
3. **Переменные окружения** - все переменные остаются теми же
4. **Структура проекта** - все пути относительные, не зависят от имени репозитория
5. **.gitignore** - уже настроен правильно

## 🔄 Что нужно сделать:

### 1. Изменить remote репозитория

```bash
# Удалить старый remote
git remote remove origin

# Добавить новый remote
git remote add origin https://github.com/ahmed11551/MubarakWay.git

# Проверить
git remote -v
```

### 2. Отправить код в новый репозиторий

```bash
# Убедитесь, что все изменения закоммичены
git status

# Отправить в новый репозиторий
git push -u origin main
```

### 3. Настроить Vercel для нового репозитория

1. Откройте: https://vercel.com/dashboard
2. Нажмите **"Add New Project"**
3. Выберите репозиторий **"ahmed11551/MubarakWay"**
4. Настройте проект:
   - **Framework Preset**: Next.js
   - **Root Directory**: `./` (по умолчанию)
   - **Build Command**: `pnpm build` (или оставьте по умолчанию)
   - **Output Directory**: `.next` (по умолчанию)

### 4. Добавить переменные окружения в Vercel

После создания проекта в Vercel:

1. Перейдите в **Settings** → **Environment Variables**
2. Добавьте все переменные:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `SUPABASE_SERVICE_ROLE_KEY`
   - `API_AUTH_TOKEN`
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_SECRET_TOKEN`
   - `BOT_API_BASE_URL`
   - `BOT_API_TOKEN`

3. Выберите окружения: **Production**, **Preview**, **Development**

### 5. Подключить Git интеграцию (опционально)

Если хотите автоматический деплой:

1. В Vercel проекте: **Settings** → **Git**
2. Нажмите **"Connect Git Repository"**
3. Выберите **"ahmed11551/MubarakWay"**
4. Подтвердите

## 📝 Опциональные изменения (для чистоты):

### Обновить README.md

Можно изменить название проекта в README.md с "SadakaPass" на "MubarakWay", но это не обязательно для работы.

### Обновить package.json

```json
{
  "name": "mubarakway",
  "version": "0.1.0",
  ...
}
```

Но это тоже опционально - проект будет работать и с текущим именем.

## ✅ Проверка после переноса:

1. **Проверьте деплой**: Откройте URL проекта в Vercel
2. **Проверьте API**: 
   ```bash
   curl https://your-new-vercel-url.vercel.app/api/stats \
     -H "Authorization: Bearer YOUR_TOKEN"
   ```
3. **Проверьте переменные окружения**: Убедитесь, что все переменные добавлены в Vercel

## 🚨 Важно:

- **Не удаляйте старый репозиторий** сразу - оставьте его как резервную копию
- **Сохраните все переменные окружения** перед переносом
- **Проверьте работу** перед отключением старого деплоя

---

**Готово!** После выполнения этих шагов проект будет работать в новом репозитории.

