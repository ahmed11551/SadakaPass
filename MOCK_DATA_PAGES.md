# 📋 Страницы с моковыми данными

## 🔴 Критичные страницы (используют моковые данные)

### 1. **Детальная страница кампании** (`app/campaigns/[id]/page.tsx`)
- ❌ **Статус**: Полностью моковые данные
- ❌ **TODO**: `// TODO: Fetch campaign from database` (строка 19)
- **Что используется**:
  - Хардкодный объект `campaign` (строки 20-58)
  - Моковые данные: title, description, story, goalAmount, currentAmount, etc.
  - Моковые обновления кампании (`updates`)
  - Моковые недавние пожертвования (`recentDonors`)
- **Нужно**: Использовать `getCampaignById(id)` из `lib/actions/campaigns.ts`

---

### 2. **Детальная страница фонда** (`app/funds/[id]/page.tsx`)
- ❌ **Статус**: Полностью моковые данные
- ❌ **TODO**: `// TODO: Fetch fund from database` (строка 19)
- **Что используется**:
  - Хардкодный объект `fund` (строки 20-48+)
  - Моковые данные: name, description, totalRaised, donorCount, etc.
  - Моковые недавние пожертвования (`recentDonations`)
  - Моковые проекты фонда (`projects`)
- **Нужно**: Использовать `getFundById(id)` из `lib/actions/funds.ts`

---

### 3. **Админ-панель - Таблица кампаний** (`components/admin/campaigns-table.tsx`)
- ❌ **Статус**: Полностью моковые данные
- ❌ **TODO**: `// TODO: Fetch real campaigns from database` (строка 9)
- **Что используется**:
  - Хардкодный массив `campaigns` (строки 10-38)
  - 3 тестовые кампании с фиксированными данными
- **Нужно**: Получить из БД через `getCampaigns()` с фильтрами по статусу

---

### 4. **Админ-панель - Таблица фондов** (`components/admin/funds-table.tsx`)
- ❌ **Статус**: Полностью моковые данные
- ❌ **TODO**: `// TODO: Fetch real funds from database` (строка 9)
- **Что используется**:
  - Хардкодный массив `funds` (строки 10-35)
  - 2 тестовых фонда с фиксированными данными
- **Нужно**: Получить из БД через `getFunds()` из `lib/actions/funds.ts`

---

### 5. **Админ-панель - Таблица пожертвований** (`components/admin/donations-table.tsx`)
- ❌ **Статус**: Полностью моковые данные
- ❌ **TODO**: `// TODO: Fetch real donations from database` (строка 7)
- **Что используется**:
  - Хардкодный массив `donations` (строки 8-39)
  - 3 тестовых пожертвования с фиксированными данными
- **Нужно**: Получить из БД через Supabase запрос к таблице `donations`

---

### 6. **Главная страница - Карусель срочных сборов** (`app/page.tsx`)
- ⚠️ **Статус**: Моковые данные для карусели
- **Что используется**:
  - Хардкодный массив `urgentCampaigns` (строки 34-71)
  - 3 тестовые кампании с фиксированными данными
- **Нужно**: Использовать реальные данные из `/api/campaigns` с фильтром по срочности
- **Примечание**: Секция "Активные кампании" уже использует реальные данные ✅

---

### 7. **Форма создания кампании** (`components/campaign-creation-form.tsx`)
- ⚠️ **Статус**: Форма есть, но не сохраняет в БД
- ❌ **TODO**: `// TODO: Implement campaign creation with server action` (строка 62)
- **Что используется**:
  - Только `console.log()` для вывода данных (строка 63)
  - Симуляция API вызова (строка 75)
- **Нужно**: Вызвать `createCampaign()` из `lib/actions/campaigns.ts`

---

## 🟡 Страницы с частично моковыми данными

### 8. **Страница профиля** (`app/profile/page.tsx`)
- ✅ **Статус**: Использует реальные данные с fallback на моковые
- **Что используется**:
  - Реальные данные через `getUserDonations()` ✅
  - Fallback на моковые данные, если API не доступен (строки 42-76)
- **Примечание**: Это нормально, fallback работает правильно

---

## ✅ Страницы с реальными данными

### 9. **Страница списка кампаний** (`app/campaigns/page.tsx`)
- ✅ Использует `getCampaigns("active")` и `getCampaigns("completed")` из Supabase

### 10. **Страница списка фондов** (`app/funds/page.tsx`)
- ✅ Использует `getFunds()` с fallback на Supabase

### 11. **Главная страница - Активные кампании**
- ✅ Использует `/api/campaigns?status=active&limit=3`

---

## 📊 Сводка

| Страница/Компонент | Статус | Приоритет |
|-------------------|--------|-----------|
| `/campaigns/[id]` | ❌ Моковые данные | 🔴 Критичный |
| `/funds/[id]` | ❌ Моковые данные | 🔴 Критичный |
| Админ: Кампании | ❌ Моковые данные | 🔴 Критичный |
| Админ: Фонды | ❌ Моковые данные | 🔴 Критичный |
| Админ: Пожертвования | ❌ Моковые данные | 🔴 Критичный |
| Главная: Карусель | ⚠️ Моковые данные | 🟡 Важный |
| Форма создания | ⚠️ Не сохраняет | 🔴 Критичный |

---

## 🎯 Приоритет исправления

### Высокий приоритет (для работы приложения):
1. Детальная страница кампании
2. Детальная страница фонда
3. Форма создания кампании
4. Админ-таблицы (все 3)

### Средний приоритет:
5. Карусель на главной странице

---

**Последнее обновление**: 2025-01-15

